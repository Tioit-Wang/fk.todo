{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "quick", "reminder"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
